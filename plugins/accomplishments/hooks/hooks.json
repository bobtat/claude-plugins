{
  "description": "Distils each session transcript into a small redacted digest of the prompts you typed, before Claude Code's 30-day retention deletes the transcript. Assistant replies, tool calls, tool output, file contents, and injected context are never written. Inert until /accomplishments:init creates the journal; silent, non-blocking, and fails open.",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/archive-session.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
