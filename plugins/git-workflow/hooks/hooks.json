{
  "description": "Blocks git commands that destroy unrecoverable work, force-push over collaborators, or hang waiting on an interactive editor.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/guard-git.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
